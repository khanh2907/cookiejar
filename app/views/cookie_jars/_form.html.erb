<%= form_for(@cookie_jar) do |f| %>
  <% if @cookie_jar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cookie_jar.errors.count, "error") %> prohibited this cookie_jar from being saved:</h2>

      <ul>
      <% @cookie_jar.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :level %><br />
    <%= f.number_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
